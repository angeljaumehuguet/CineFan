<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    
    <!-- Color de fondo del chip -->
    <solid android:color="@color/cinefan_accent" />
    
    <!-- Esquinas redondeadas -->
    <corners android:radius="12dp" />
    
    <!-- Padding interno -->
    <padding
        android:left="8dp"
        android:top="4dp"
        android:right="8dp"
        android:bottom="4dp" />
    
    <!-- Borde opcional -->
    <stroke
        android:width="1dp"
        android:color="@color/cinefan_accent" />
        
</shape>